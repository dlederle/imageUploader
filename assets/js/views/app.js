var app = app || {};

$(function($) {
  'use strict';

  app.AppView = Backbone.View.extend({
    el: '.container',

    events: {
    },

    initialize: function() {
    },

    render: function() {
      return this;
    }

  });
});
